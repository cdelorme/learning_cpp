
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {

    // we can define a string like this
    //string line = "whee";

    // also has a constructor so we can run it with ()
    string line("Whee");

    cout << line << endl;

    return 0;
}

