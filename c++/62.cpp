
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {

    // fs options gives us powah!
    // standards include ios::in and ios::out, both self-explanitory
    // we also have append aka ios::app
    // then there is ios.ate, which gives us a spot at the end of the file to read and write
    // ios::trunc will erase the contents, but you still have both input and output access
    // finally (my favorite) is ios::binary, it will write raw text and ignore special characters such as \n
    // you can also supply multiple arguments with | separating them


    return 0;
}

