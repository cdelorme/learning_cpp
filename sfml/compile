#!/bin/bash

FILE=$1

if [ -z "$FILE" ]
then

    ##
    # compile all
    ##

    # build all cpp to o
    for FILE in *.cpp
    do
        g++-4.8 -Og -c $FILE
    done

    # compile to executable
    for FILE in *.o
    do
        g++-4.8 -Og -framework SFML -lsfml-graphics -lsfml-audio -lsfml-window -lsfml-system -o ${FILE%%.*} $FILE
        rm -rf $FILE
    done

elif [ "$FILE" = "clean" ]
then

    echo $0
    echo "Cleaning files"
    for FILE in ./*
    do
        if [ -x $FILE ] && [ "${FILE}" != "$0" ]
        then
            # remove executable files
            rm -rf $FILE
        fi
    done

else

    ##
    # compile the specified file
    ##

    if ! [ -f "$FILE.cpp" ]
    then
        echo "file not found"
        exit 1
    fi

    # build
    g++-4.8 -Og -c "${FILE}.cpp"

    # compile
    g++-4.8 -Og -framework SFML -lsfml-graphics -lsfml-audio -lsfml-window -lsfml-system -o $FILE "${FILE}.o"

    # cleanup
    rm -rf "${FILE}.o"

    # run
    ./$FILE

fi

